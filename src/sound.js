// sound.js
// Stub audio module — all calls are no-ops in Phase 6.
// Phase 7 will replace this with real WebAudio implementations.
//
// Planned sound names:
//   'throw'      — the moment a player releases the projectile
//   'explosion'  — any hit (building or character)
//   'roundWin'   — plays alongside a character-hit explosion
//   'matchWin'   — plays when the match banner appears
//   'miss'       — projectile leaves the canvas without hitting anything
export function playSound(name) {
  // No-op until Phase 7.
}
